@model IEnumerable<AppDevelopment0805.ViewModels.CourseTrainersViewModel>
@{
    ViewBag.Title = "GetTrainers";
}

<h2>Trainers in courses</h2>

<div>
    @Html.ActionLink("Assign to Trainer", "AddTrainer", "Courses", new { @class = "btn btn-default" })
    @Html.ActionLink("Remove to Trainer", "RemoveTrainer", "Courses", new { @class = "btn btn-default" })
</div>

<div></div>

@using (Html.BeginForm("GetTrainers", "Courses", FormMethod.Get))
{
    <div class="form-group">
        @Html.TextBox("SearchCourse", null, new { @class = "form-control" })
    </div>
    <button type="submit" class="btn btn-default">Search</button>
}

<table class="table">
    <thead class="thead-dark">
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Trainers</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in @Model)
        {
            <tr>
                <td>@item.Course.Name</td>
                <td>
                    <ul>
                        @foreach (var trainer in item.Trainers)
                        {
                            <li>@trainer.Email</li>
                        }
                    </ul>
                </td>
            </tr>
        }
    </tbody>
</table>