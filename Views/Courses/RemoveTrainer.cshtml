@model AppDevelopment0805.ViewModels.TrainersCourseViewModel
@{
    ViewBag.Title = "Remove to Trainer";
}

<h2>Remove Trainer from Course</h2>

@using (@Html.BeginForm("RemoveTrainer", "Courses", FormMethod.Post))
{
    @Html.ValidationSummary("", new { @class = "text-danger" })

    <div class="form-group">
        <label>Course</label>
        @Html.DropDownListFor(m => m.CourseId,
        new SelectList(Model.Courses, "Id", "Name"),
        new { @class = "form-control" }
        )
    </div>

    <div class="form-group">
        <label>Trainer</label>s
        @Html.DropDownListFor(m => m.TrainerId,
       new SelectList(Model.Trainers, "Id", "Email"),
       new { @class = "form-control" }
       )
    </div>
    <button type="submit" class="btn btn-default">Remove</button>
}